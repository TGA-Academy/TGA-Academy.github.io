# RavenGrappling

# Local Dev
- terminal: `bundle exec jekyll serve`
- browser: `http://localhost:4000`

# How to push (terminal)
- `git status` - shows changes
- `git add .`  - adds files to push
- `git commit -m "This is a commit messages"`
- `git push` - goes to github and get's released live
